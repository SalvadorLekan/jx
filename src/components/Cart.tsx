function Cart() {
  return (
    <div
      onClick={() => {}}
      className={`bg-opacity-20 row justify-content-end top-vh fixed cart-store right-0 left-0 bottom-0 vh-100`}
    ></div>
  );
}

export default Cart;
